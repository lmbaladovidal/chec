module.exports = (sequelize, dataTypes) => {
    let alias = 'Recipes';
    let cols = {

    };
    let config = {
        timestamps: false
    }

}

const Recicpe = sequelize.define(alias, cols, config)