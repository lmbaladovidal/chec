module.exports = 
    (sequelize,DataTypes)=>{
        let alias = "Products"
        let columns = {}
        let config ={}
        const Products = sequelize.define(alias,columns,config);
        //asociaciones
    }
